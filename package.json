{
  "name": "mcp-usage-control",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.15.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -b",
    "test": "pnpm -r test",
    "check": "pnpm build && pnpm test"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
