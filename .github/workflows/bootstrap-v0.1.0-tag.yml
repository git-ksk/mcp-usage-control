name: Bootstrap final v0.1.0 tag

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/bootstrap-v0.1.0-tag.yml'

permissions:
  contents: write

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Tag the final tested release commit exactly once
        env:
          TARGET: 9b40abb8739f50949c7819d2314814ffbea8c8da
          TAG: v0.1.0
        shell: bash
        run: |
          set -euo pipefail
          git fetch --tags --force
          if git rev-parse -q --verify "refs/tags/$TAG" >/dev/null; then
            existing="$(git rev-list -n 1 "$TAG")"
            test "$existing" = "$TARGET"
            echo "$TAG already points to $TARGET"
            exit 0
          fi
          git cat-file -e "$TARGET^{commit}"
          git tag "$TAG" "$TARGET"
          git push origin "refs/tags/$TAG"
