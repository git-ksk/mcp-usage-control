# Benchmark baselines

These JSON files are reproducible environment-specific smoke evidence generated by `scripts/benchmark-usage-store.mjs`. They are not SLOs, price guarantees, or provider rankings. Compare files only when runtime, provider version/topology, iteration count, and concurrency are sufficiently similar.

See [Provider benchmark and cost-profile harness](../provider-benchmarks.md).
